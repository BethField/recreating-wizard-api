# Recreating Wizard API

Recreating the WizardWorld Api

## Installation

Run `npm install -D` from the `wizard-api/` folder to install required packages

## Development

Run `npm run dev` from the `wizard-api/` folder to run server
